---
- name: Create owner
  user:
    name: "{{ ds_owner }}"
    shell: /bin/bash
    system: true

- name: Create home
  file:
    path: "{{ item }}"
    state: directory
    owner: "{{ ds_owner }}"
    group: "{{ ds_group }}"
    mode: 0755
  with_items:
    - "{{ ds_home }}"
    - "{{ ds_home_version }}"
    - "{{ ds_home_version }}/downloads"

- name: Home link
  file:
   src: "{{ ds_home_version }}"
   dest: "{{ ds_home_link }}"
   state: link

- include: download.yml
- include: install.yml
